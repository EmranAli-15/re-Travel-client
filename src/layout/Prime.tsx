import { Outlet } from 'react-router-dom'

const Prime = () => {
        return (
                <div>
                        <Outlet></Outlet>
                </div>
        );
};

export default Prime;